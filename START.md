# 我爱克隆 - 启动指南

## 🚀 快速启动

### 方式一：分别启动（推荐用于开发）

#### 1. 启动后端服务（Flask）

打开第一个终端：

```bash
cd WebTwin
.\venv\Scripts\activate
python app.py
```

后端将运行在：http://127.0.0.1:5001

#### 2. 启动前端服务（Next.js）

打开第二个终端：

```bash
cd WebTwin/frontend
npm install  # 首次运行需要安装依赖
npm run dev
```

前端将运行在：http://localhost:3000

### 方式二：首次安装

如果是第一次运行项目，请按以下步骤操作：

#### 1. 安装 Python 依赖

```bash
cd WebTwin
python -m venv venv
.\venv\Scripts\activate
pip install -r requirements.txt
pip install html5lib  # 确保安装了 html5lib
```

#### 2. 安装 Node.js 依赖

```bash
cd frontend
npm install
```

#### 3. 启动服务

按照"方式一"的步骤启动后端和前端服务。

## 📝 使用说明

1. 访问 http://localhost:3000
2. 点击"免费开始使用"按钮
3. 在弹出的对话框中输入要克隆的网站地址
4. 勾选"使用高级渲染"（推荐用于现代网站）
5. 点击"开始克隆"
6. 等待处理完成，自动下载 ZIP 文件

## 🔧 故障排除

### 后端问题

**问题：缺少 html5lib**
```bash
pip install html5lib
```

**问题：Selenium 无法启动**
- 确保已安装 Chrome 浏览器
- Selenium 会自动下载 ChromeDriver

### 前端问题

**问题：端口被占用**
```bash
# 修改端口，在 frontend 目录下运行：
npm run dev -- -p 3001
```

**问题：API 连接失败**
- 确保后端服务正在运行（http://127.0.0.1:5001）
- 检查 next.config.js 中的 API 代理配置

## 🎯 功能特性

- ✅ 完整提取 HTML、CSS、JavaScript
- ✅ 支持图片、字体、视频等资源
- ✅ 智能识别 React、Vue、Next.js 等框架
- ✅ Selenium 高级渲染模式
- ✅ 自动打包成 ZIP 文件
- ✅ 美观的现代化界面

## 📦 生产部署

### 构建前端

```bash
cd frontend
npm run build
npm start
```

### 生产环境运行后端

```bash
# 使用 gunicorn（推荐）
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5001 app:app
```

## 🌟 技术栈

- **前端**: Next.js 14 + TypeScript + Tailwind CSS + Shadcn/ui
- **后端**: Flask + BeautifulSoup + Selenium
- **渲染**: Chrome WebDriver

## 📄 License

MIT License - 完全开源免费
